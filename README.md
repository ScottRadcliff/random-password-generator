## This is a simple attempt at creating and packaging a Node module

Install

    $ npm install random-password-generator

There is not much to this. Just a `generate` method that returns a random string
of 8 characters.


Usage

    var generator = require('random-password-generator');
    generator.generate();
    #=> qkqtuflq 
  



